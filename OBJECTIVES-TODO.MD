- global variables
- types props for entities
- process pipelines (crud like operations)
- global process pipelines (crud like operations)
- faster and easy multi platform (mobile and pc) controller creation (events like pointers and keyboard)
- API to handle loading (async and blocking)
    NOTE:   idk if make sense or should be something external, it seems solvable with global process pipeline
            but if I have to do it everytime maybe it should be automated
- scene loader
- debug mode 
    NOTE:   something actually usefull so this will be the last implementation